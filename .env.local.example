# Public site URL (used for absolute links if needed; safe to leave default)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# RSVP storage (optional — without this, RSVPs are logged to server console only)
POSTGRES_URL=
